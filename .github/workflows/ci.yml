name: CI

on:
  push:
  pull_request:

jobs:
  plugins:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: plugins/agent-keep-awake
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: plugins/agent-keep-awake/package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm run typecheck
